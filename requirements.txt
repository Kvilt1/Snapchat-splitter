# Snapchat Media Mapper - Python Dependencies
# ===============================================
# Note: This tool also requires ffmpeg to be installed on your system
# See README.md for ffmpeg installation instructions

# Core video/media processing
ffmpeg-python>=0.2.0       # Python bindings for ffmpeg
imageio[ffmpeg]>=2.9.0     # Image and video I/O
Pillow>=10.0.0             # Image processing (WebP conversion)
numpy>=1.19.0              # Required by imageio

# Progress tracking and UI
tqdm>=4.65.0               # Progress bars

# System monitoring and optimization
psutil>=5.8.0              # System resource detection (CPU, RAM)

# Timezone handling
pytz>=2023.3               # Timezone conversions (Faroese timezone)

# Optional: Advanced GPU monitoring
# Uncomment below to enable NVIDIA GPU monitoring via Python
# pynvml>=11.0.0           # NVIDIA Management Library (optional)
